"""V2 컨트롤러 내부 부품 실측 스펙 (사용자 제공 2026-06). 단위 = mm.
하우징/마운트 설계 스크립트가 이 상수들을 import → 치수는 여기 한 곳에서만 관리.
좌표계: 원본 Quest 좌컨트롤러 STEP 프레임 (버튼/스틱 XY가 그 프레임 기준, side="right").
"""

# ── IMU 보드 (WT901C485, 보드형, 케이스 없음) ──
IMU_BOARD = (34.0, 30.0, 10.0)     # 세로34 × 가로30 × 두께10 (보드 자체)
IMU_TILT_DEG = 18.0               # 손잡이 축과 정렬(수납 위해 30→18)
# 마운트 = 보드 대각선 Ø2 홀 2개 (34=가로 기준, 좌하단 코너로부터 (가로,세로)mm)
IMU_HOLE_DIA = 2.0
IMU_MOUNT_HOLES = [(3.0, 6.5), (31.0, 23.5)]   # 대각선 2개

# ── 조이스틱 KY-023 (주행, 엄지) ──
JOY_SIZE = (40.0, 26.0, 35.0)     # L × W × H(높이)
JOY_POS = (8.5, 1.5)             # 썸스틱 위치 (컨트롤러 프레임 XY)
# 실측(KY-023): 마운트홀 중심간 가로23.4 × 세로29.1. 깊이(세로29.1)를 device-X로 눕혀 헤드 Y 절약(가로23.4=Y) → 펌웨어서 VRx/VRy 스왑
JOY_MOUNT_GRID = (29.1, 23.4)     # (gx=X, gy=Y) — 90° 눕힘
JOY_DOME_DIA = 18.0              # 돔 Ø
JOY_SIDE = "right"               # 우측 그립만
# 옵션: 스틱 지지 칼라 추가, 또는 저프로파일 썸스틱으로 대체

# ── 택트 스위치 (A / B / 메뉴, 6×6) ──
TACT = (6.0, 6.0)
TACT_POCKET = 6.4                # 사각 포켓 한 변
BTN_POS = {"A": (-12.0, 2.0), "B": (-8.0, 16.0), "menu": (1.0, 33.0)}  # layout 기준 XY

# ── 트리거 = 그리퍼 센싱 (검지) ──
HALL = "AH49E"                   # 아날로그 홀센서, 하우징 고정
MAGNET_DIA = 5.0                 # 네오디뮴 디스크 Φ5
MAGNET_THK = 2.0                 # × 2mm 두께 (레버에 부착)
TRIG_GAP = (1.0, 3.0)           # 자석-홀 갭 범위
TRIG_PIVOT_DIA = 3.0            # 힌지축: Φ3 평행핀 또는 M3 볼트(택1)
TRIG_SPRING_ID = (3.5, 4.0)     # 토션스프링 내경, 또는 ↓ 리빙힌지
LIVING_HINGE_T = (0.8, 1.2)     # PETG 리빙힌지 두께 대안

# ── 리프트 (중지) ──
LIFT_SWITCH = "SS-5GL"          # 리미트스위치 19.8×6.4×10.2, 나사홀 Ø2.4 피치9.5(6.4두께 관통). 중지 레버가 액추에이터 누름+토션스프링 복귀

# ── 공통 프린트 파라미터 ──
WALL = 5.0                       # 셸 외벽 (설계 메모 기준)
FDM_CLEAR = 0.3                  # 끼움 공차(FDM, 부품 포켓 한 변당)
SNAP_CLEAR = 0.15                # snap-fit 클립 공차
